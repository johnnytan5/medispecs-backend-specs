fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic>=2.10.0  # Updated for Python 3.13 compatibility
pydantic-settings>=2.6.0  # Updated for Python 3.13 compatibility
python-multipart==0.0.6
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-dotenv==1.0.0
requests==2.31.0
sqlalchemy>=2.0.35  # Updated for Python 3.13 compatibility
greenlet>=3.0.0  # Required by SQLAlchemy async engine
httpx==0.25.2
aiosqlite==0.19.0
Pillow>=10.2.0
luma.oled>=3.12.0
opencv-python-headless>=4.8.1.78
ultralytics>=8.0.227

# Text-to-Speech (Hybrid: gTTS primary, pyttsx3 fallback)
gTTS==2.5.1  # Google Text-to-Speech (high quality, requires internet)
pygame==2.5.2  # Audio playback for gTTS
pyttsx3==2.90  # Offline TTS fallback (basic quality)

# Speech-to-Text (Vosk - offline speech recognition)
vosk==0.3.45  # Offline speech recognition
sounddevice==0.4.6  # Audio recording from microphone
numpy>=1.24.0  # Required for audio processing

# LLM (OpenAI GPT for voice command processing)
openai>=1.3.0  # OpenAI API client

# Timelapse Recording & Video Processing
ffmpeg-python==0.2.0  # FFmpeg wrapper for video creation

# picamera2 - Not in requirements, installed separately for Docker/Pi
# Note: On Raspberry Pi, also install system packages:
#   sudo apt-get install espeak espeak-ng           # For TTS
#   sudo apt-get install ffmpeg                     # For timelapse video creation